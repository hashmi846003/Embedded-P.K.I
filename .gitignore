# Generated certs, keys, and the certificate database — regenerate with:
#   certctl init-ca && certctl issue ...
certs/
data/
*.key
*.db

# Built Go binaries (run go build in server/)
server/certctl
server/server
